Generic visualization GUI instead of anticipating what the user wants beforehand. DONE
LLM integration to convert pubmed queries to scopus. DONE
LLM integration to let the user enter an unformatted query and convert that to scopus. DONE
Are there options ohter than Scopus for impact factors? (no other options for impact factors, other metrics exist but impact factor and citation counts is what we decided to use.)
